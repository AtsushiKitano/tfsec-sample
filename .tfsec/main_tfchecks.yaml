checks:
  - code: CUS001
    description: "Subnetwork region is japan"
    requiredTypes:
      - resource
    requiredLabels:
      - google_compute_subnetwork
    servirity: ERROR
    matchSpec:
      name: region
      action: contains
      value: asia-northeast
    errorMessage: "Subnetwork Region is not asia-northeast1 or asia-northeast2 region"
